[
  {
    "title": "Implement Training Page in Dashboard",
    "task": "You are working on the Blindsight project at c:/Users/josee/blindsight.\n\n**Task:** Implement a Training Modules page in the Dashboard SPA at `web/apps/dashboard/src/`.\n\n**Backend endpoints:**\n- `GET /api/v1/training/modules` \u2014 Returns list of training modules (each with id, title, description, duration, category, difficulty)\n- `GET /api/v1/training/progress` \u2014 Returns user's progress per module (completed, percentage, last_accessed)\n- `POST /api/v1/training/progress` \u2014 Updates progress for a module\n\n**Backend data structure (from src/api/v1/training/router.py):**\nThe backend returns `TRAINING_MODULES_DATA` which is a hardcoded list of modules. Read the router file to understand the exact shape.\n\n**What you need to do:**\n1. Read `src/api/v1/training/router.py` to understand the module and progress data shapes\n2. Read `web/apps/dashboard/src/app/app.tsx` for routing patterns\n3. Read existing pages for structure/style (PrivacyPage, ProfilePage)\n4. Create `web/apps/dashboard/src/services/training.ts`:\n   - `getModules()` -> GET /api/v1/training/modules\n   - `getProgress()` -> GET /api/v1/training/progress\n   - `updateProgress(moduleId, percentage)` -> POST /api/v1/training/progress\n5. Create `web/apps/dashboard/src/pages/TrainingPage.tsx`:\n   - Display list of training modules as cards (title, description, duration, difficulty, category)\n   - Show progress bar per module (from progress API)\n   - \"Mark Complete\" button per module -> calls updateProgress\n   - Follow existing glassmorphism styling\n6. Add route in `app.tsx` at `/app/training` inside `<ProtectedRoute>`\n7. Add React.lazy() import\n8. Add sidebar navigation link in `AppShell.tsx` (use `BookOpen` icon from lucide-react)\n\n**Important:**\n- Read files first, don't guess\n- Follow existing API client and error handling patterns\n- Run typecheck after: `cd c:\\Users\\josee\\blindsight\\web ; npx tsc --noEmit --project apps/dashboard/tsconfig.json`\n\nReturn a summary of all files created/modified."
  },
  {
    "title": "Implement Groups Management Page in Dashboard",
    "task": "You are working on the Blindsight project at c:/Users/josee/blindsight.\n\n**Task:** Implement a Groups management page in the Dashboard SPA at `web/apps/dashboard/src/`.\n\n**Backend endpoints:**\n- `POST /api/v1/groups/create` \u2014 Create a new group (name, description)\n- `POST /api/v1/groups/update` \u2014 Update group details\n- `POST /api/v1/groups/invite` \u2014 Invite a user to a group (email, group_id)\n- `GET /api/v1/groups/check-invitation/{token}` \u2014 Check invitation validity\n- `POST /api/v1/groups/accept-invitation/{token}` \u2014 Accept invitation\n- `POST /api/v1/groups/remove-member` \u2014 Remove member from group\n- `POST /api/v1/groups/leave` \u2014 Leave a group\n\n**What you need to do:**\n1. Read `src/services/group_service.py` and `src/api/v1/groups/router.py` to understand the exact request/response shapes\n2. Read `web/apps/dashboard/src/app/app.tsx` for routing patterns\n3. Read existing pages for structure/style (PrivacyPage, ProfilePage)\n4. Create `web/apps/dashboard/src/services/groups.ts`:\n   - `createGroup(data)`, `updateGroup(data)`, `inviteMember(data)`, `removeMember(data)`, `leaveGroup(data)`\n   - `checkInvitation(token)`, `acceptInvitation(token)`\n5. Create `web/apps/dashboard/src/pages/GroupsPage.tsx`:\n   - \"Create Group\" form (name, description)\n   - List of user's groups with expand/collapse for members\n   - \"Invite Member\" input per group (email)\n   - \"Leave\" or \"Remove Member\" buttons\n   - Follow existing glassmorphism styling\n6. Add route in `app.tsx` at `/app/groups` inside `<ProtectedRoute>`\n7. Add React.lazy() import\n8. Add sidebar navigation link in `AppShell.tsx` (use `Users` icon from lucide-react, or `UserGroup` if available)\n\n**Important:**\n- Read files first, don't guess\n- Follow existing API client and error handling patterns\n- Run typecheck after: `cd c:\\Users\\josee\\blindsight\\web ; npx tsc --noEmit --project apps/dashboard/tsconfig.json`\n\nReturn a summary of all files created/modified."
  },
  {
    "title": "Implement Security Reports Admin Page",
    "task": "You are working on the Blindsight project at c:/Users/josee/blindsight.\n\n**Task:** Implement a Security Reports management page in the Blindsight Admin SPA at `web/apps/blindsight-admin/src/`.\n\n**Backend endpoints:**\n- `GET /api/v1/security-reports/` \u2014 List all security reports (paginated)\n- `GET /api/v1/security-reports/{report_id}` \u2014 Get single report\n- `POST /api/v1/security-reports/` \u2014 Create a new report\n- `POST /api/v1/security-reports/status/update` \u2014 Update report status\n- `POST /api/v1/security-reports/mitigation/add` \u2014 Add mitigation step\n- `POST /api/v1/security-reports/investigation/add` \u2014 Add investigation note\n- `POST /api/v1/security-reports/verification/update` \u2014 Update verification status\n- `POST /api/v1/security-reports/preventive/add` \u2014 Add preventive measure\n\n**What you need to do:**\n1. Read `src/api/v1/security_reports/router.py` to understand exact request/response shapes (StatusUpdateRequest, MitigationStepRequest, etc.)\n2. Read `src/services/security_report_service.py` if it exists\n3. Read `web/apps/blindsight-admin/src/app/app.tsx` for routing patterns\n4. Read existing admin pages (UsersPage, GroupsPage) for structure/style\n5. Create `web/apps/blindsight-admin/src/services/securityReports.ts`:\n   - `list()`, `get(id)`, `create(data)`, `updateStatus(data)`, `addMitigation(data)`, `addInvestigation(data)`, `updateVerification(data)`, `addPreventive(data)`\n6. Create `web/apps/blindsight-admin/src/pages/SecurityReportsPage.tsx`:\n   - List of security reports (title, status, severity, created date)\n   - \"Create Report\" form (title, description, severity, category)\n   - Detail view per report with:\n     - Status dropdown + update button\n     - Mitigation steps list + add form\n     - Investigation notes list + add form\n     - Verification status + update\n     - Preventive measures list + add form\n   - Follow existing admin styling\n7. Add route in `app.tsx` at `/security-reports` inside `<AdminRoute>`\n8. Add import (can be direct or lazy)\n9. Add sidebar navigation link in the Layout component (use `Shield` icon from lucide-react)\n\n**Important:**\n- Read files first, don't guess\n- Follow existing API client and error handling patterns\n- Run typecheck after: `cd c:\\Users\\josee\\blindsight\\web ; npx tsc --noEmit --project apps/blindsight-admin/tsconfig.json`\n\nReturn a summary of all files created/modified."
  }
]